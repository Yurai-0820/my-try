
  
  <%# <% if current_page?(current_user) %> 
  <% @limits.each do |limit| %>
    <% if current_user.id == limit.user_id %> 

      <div class="max-weight-contents">

              <ul class="max-weight-wrap">
              <li class="max-set-weight-1">
                  <span class = "max-weight" >今のMAX重量は</span>
                  <% if limit.rep > 1 %>
                  <%= (limit.max_weight * limit.rep / 40 + limit.max_weight)  %>kg
                  <% else %>
                  <%= limit.max_weight %>kg
                  <% end %>
              </li>

        <div class = "set-menus" >
          <div class = "set-menu" >

                <div class="max-weight">
                  セットメニュー
                </div>
                </div>


            <div class = "set-weight" >


                <li class="max-set-weight">
                  メインセット　 <%= ((limit.max_weight * limit.rep / 40 + limit.max_weight) * 0.85).round %>kg  　8回
                </li> 
                <li class="max-set-weight">
                  セカンドセット <%= ((limit.max_weight * limit.rep / 40 + limit.max_weight) * 0.75).round %>kg 　10回
                </li>
                <li class="max-set-weight">
                  サードセット　 <%= ((limit.max_weight * limit.rep / 40 + limit.max_weight) * 0.65).round %>kg 　12回
                </li>
                </div>
                </div>

                </ul>
                </div>

<div class='today-main-set'><%= link_to '戻る', users_path %></div>

  <% end %>
  <% end %>

<div class="container-fluid">
	<h2 class="section-top"><span class="fas fa-dumbbell" aria-hidden="true"></span> トレーニングの記録</h2>
	<div class="col-md-3 col-sm-12 col-xs-12">
   <%= form_for (@memo) do |f| %>
     <div class="field row">
       <label for="menu">トレーニング名</label>
       <p><%= f.text_area :menu%></p>
       <label for="weight">重量(kg)</label>
       <p><%= f.select :weight, [*(0..200)]%></p>
       <label for="rep">回数</label>
       <p><%= f.select :rep, [*(1..100)]%></p>
       <label for="set">セット数</label>
       <p><%= f.select :set, [*(1..30)]%></p>
       <label for="start_time">トレーニング日</label>
       <p><%= f.datetime_select :start_time%></p>
     </div>
     <div class="actions row">
       <%= f.submit class: "btn btn-sm btn-hachiss" %>
     </div>
   <% end %>
 	</div>
 	<div class="col-md-9 col-sm-12 col-xs-12">
		<%= month_calendar events: @memos do |date, memos| %>
		  <%= date.day %>
		  <% memos.each do |memo| %>
		    <div>
		      <%= link_to memo.menu, edit_memo_path(memo) %>
		    </div>
		  <% end %>
		<% end %>
	
  		<%= paginate @memos %>
 	</div>
</div>  
